---
# defaults file for criecm.nfs_server
- name: rc conf
  lineinfile:
    line: '{{ item }}'
    regexp: "^{{ item | regex_replace('=.*$','=') }}"
    dest: /etc/rc.conf
  with_items:
    - 'nfs_server_enable="YES"'
  notify: restart freebsd nfs server
  when: ansible_os_family == "FreeBSD"


