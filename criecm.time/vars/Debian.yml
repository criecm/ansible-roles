---
distrib_time_client_config: '{% if not "container" in ansible_virtualization_tech_guest %}/etc/chrony/chrony.conf{% else %}False{% endif %}'
distrib_time_client_service: '{% if not "container" in ansible_virtualization_tech_guest %}chrony{% else %}False{% endif %}'
distrib_time_client_package: '{% if not "container" in ansible_virtualization_tech_guest %}chrony{% else %}False{% endif %}'
