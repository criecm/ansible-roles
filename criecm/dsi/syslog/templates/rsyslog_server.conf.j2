{% if syslog_server | default(False) %}
# ansible: syslog_server={{ syslog_server }}
auth,authpriv.*                 @{{ syslog_auth_server | default(syslog_server) | ansible.utils.ipwrap }}
mail.info                       @{{ syslog_server | ansible.utils.ipwrap }}
{% else %}
# ansible: no syslog_server
{% endif %}
