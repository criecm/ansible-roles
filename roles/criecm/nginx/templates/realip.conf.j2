# {{ ansible_managed }}
map $http_x_forwarded_proto$scheme $myhttps {
        default   '';
        https    on;
	httpshttp       on;
        httpshttps      on;
}

map $http_x_forwarded_proto$scheme $myscheme {
	default		http;
	httpshttp	https;
	httpshttps	https;
	https		https;
}

map $http_x_forwarded_proto $myport {
	default		$server_port;
	http		80;
	https		443;
}
